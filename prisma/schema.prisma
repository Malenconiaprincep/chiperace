model Banner {
  id          Int      @id @default(autoincrement())
  title       String
  subtitle    String
  description String
  image       String
  link        String
  createdAt   DateTime @default(now())
} 